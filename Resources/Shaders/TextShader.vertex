#version 330

layout(location=0) in vec4 in_Position;

out vec2 texture_Coord;

uniform mat4 projection;

void main(void)
{
	gl_Position = projection * vec4(in_Position.xy, 0.0, 1.0f);
	texture_Coord = in_Position.zw;
}